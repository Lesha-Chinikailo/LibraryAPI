spring:
  application:
    name: library-service
  cloud:
    compatibility-verifier:
      enabled: false
    config:
      uri: http://localhost:8001
      fail-fast: true
      password: admin
      username: user
  liquibase:
    enabled: false